{
  "name": "skillmesh-backend",
  "version": "0.2.0",
  "description": "SkillMesh Phase 2 backend — AI team builder, projects, trust, opportunities, messaging, organizations. Zero external dependencies (Node.js builtins only).",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js"
  },
  "engines": { "node": ">=18" }
}
